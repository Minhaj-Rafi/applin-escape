Applin Escape 5.3.1 challenge-sharing release candidate
Linux / Python 3.12.14 / Pygame 2.6.1

162 automated tests passed.
Source preview: 52 screens at 800x600, 1280x720 and 1600x900.
Frozen Linux preview: 52 screens at the same three sizes.
Bundled audio, controller imports and asset loading passed.
PyInstaller Linux build passed.

Controller coverage:
- SDL-mapped polling and unmapped joystick fallback
- DualSense face-button order, left stick and D-pad
- one action per press
- disconnect detection without controller device events
- solo/co-op player assignment and saved bindings
- event-queue conversion recovery and keyboard-only fallback
- automated build isolation from physically attached devices

Save backup rotation, challenge replay compatibility, all five biome maps,
shiny run-only state and the remaining game suites also passed.

Challenge-sharing coverage:
- direct Challenge code entry opens as a blank, focused field
- a newly shared code is visible and selected instead of disappearing
- the latest generated code remains recoverable from the Challenge screen
- mouse/keyboard selection, copy, paste and replacement are covered

Release packaging coverage:
- verified Windows-layout builds include the executable, release notes and check manifest
- incomplete release previews are rejected before a candidate ZIP can be created
- the multiresolution Windows icon and draft-release workflow are present

Native Windows USB/Bluetooth controller testing was not available in this Linux
environment. Run BUILD_WINDOWS.bat and use Settings > Controls > INPUT TEST on
the affected PC before promoting the candidate to a stable release.
